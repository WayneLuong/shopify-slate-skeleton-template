<div>
    <div>
        <h1>{{article.title}}</h1>
    </div>
    <h4>by {{article.author}}</h4>
    <p>{{article.content}}</p>
    {{ article.created_at | date: "%a, %b %d, %y" }}
    <p>test: {{settings.blog-text}}</p>
    
</div>